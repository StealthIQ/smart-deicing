// Compatibility note: the compile-ready Arduino sketch is `arduino/arduino.ino`
// so the sketch filename matches the folder name for CLI-based builds.
// This file remains here because the original scaffold requested `arduino/main.ino`.
